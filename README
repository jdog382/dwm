Requirements:
In order to build dwm you need the Xlib header files.

terminal emulator used by default is kitty

------------
Installation:
enter the following command to build and install dwm (as root if necessary):

    make clean install

-----------
Running my dwm build:
this is the .xinitrc i use so just copy it

while true; do
     xsetroot -name "$(date +"%I:%M %p - %A %d %Y")"
     sleep 1m
done &

xhost +
exec dwm
