Requirements:
In order to build dwm you need the Xlib header files.

terminal emulator used by default is kitty, mod key is the windows key (command key for mac)

------------
Installation:
enter the following command to build and install dwm (as root if necessary):

    make clean install

-----------
Running my dwm build:
put 'exec dwm' somewhere in your .xinitrc
